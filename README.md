# Meridian Recruiter Tracker
Part of the Meridian open-source suite. Node/Express recruiter outreach tracker. Deploy to Railway, data persists via JSON file (add a Volume at /app/data for persistence across redeploys).
## Run locally
npm install && npm start
